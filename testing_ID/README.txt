NOTE:

IF testNow is True i.e. using the learnt models for realtime FF torque prediction, use the visualise.sh script in /processData

Else, use the 'data_processing' and 'learning' packages on the data recorded during testing (similar to learning, except no training this time)
